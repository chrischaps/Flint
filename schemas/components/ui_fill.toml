[component.ui_fill]
value = { type = "f32", default = 1.0, description = "Fill amount from 0.0 (empty) to 1.0 (full)" }
direction = { type = "string", default = "left_to_right", description = "Fill direction: left_to_right, right_to_left, bottom_to_top, top_to_bottom" }
