generator = "texture_v1"

[meta]
name = "crypt_floor"
description = "Dark wet stone floor for the Sunken Crypt"
version = "1.0.0"
tags = ["texture", "stone", "floor", "crypt", "wet"]

[seed]
mode = "derived"
derive_from = "crypt:floor"

[params]
pattern = "perlin_organic"
width = 512
height = 512
seamless = true
output_maps = ["albedo", "normal", "roughness"]

noise_type = "simplex"
frequency = 0.9
octaves = 3
base_color = "#3A3530"
color_variation = 0.15
roughness_base = 0.45
roughness_variation = 0.12
normal_strength = 1.1
detail_scale = 35.0
detail_strength = 0.1
