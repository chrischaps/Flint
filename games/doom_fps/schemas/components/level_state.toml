[component.level_state]
description = "Level-wide completion tracking — kills, secrets, time, and completion flag"

[component.level_state.fields]
completed = { type = "bool", default = false, description = "Whether the exit switch has been activated" }
kill_count = { type = "i32", default = 0, description = "Enemies killed (incremented by enemy AI on death)" }
total_enemies = { type = "i32", default = 0, description = "Total enemies in level (set per-scene)" }
secret_count = { type = "i32", default = 0, description = "Secrets discovered (incremented by secret_wall on interact)" }
total_secrets = { type = "i32", default = 0, description = "Total secrets in level (set per-scene)" }
elapsed_time = { type = "f32", default = 0.0, description = "Seconds elapsed since level start" }
par_time = { type = "f32", default = 0.0, description = "Par time in seconds (0 = no par)" }
