[component.secret]
description = "Push-wall secret state machine — classic Doom hidden areas"

[component.secret.fields]
discovered = { type = "bool", default = false, description = "Has this secret been found?" }
state = { type = "string", default = "closed", description = "State machine: closed, opening, open" }
slide_dx = { type = "f32", default = 0.0, description = "Total X displacement when fully open" }
slide_dz = { type = "f32", default = 0.0, description = "Total Z displacement when fully open" }
speed = { type = "f32", default = 1.5, description = "Slide speed in units/second (slower than doors for dramatic effect)" }
move_progress = { type = "f32", default = 0.0, description = "Movement progress: 0.0 = closed, 1.0 = fully open" }
closed_x = { type = "f32", default = 0.0, description = "Starting X position (captured on init)" }
closed_z = { type = "f32", default = 0.0, description = "Starting Z position (captured on init)" }
